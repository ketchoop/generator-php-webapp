<% deployer.forEach(function (server) { %>
<%= server.name %>:
    host: <%= server.host %>
    port: <%= server.port %>
    user: <%= server.user %>
    password: <%= server.pass %>
    stage: <%= server.stage %>
    deploy_path: <%= server.deployPath %>
    branch: <%= server.branch %>
<% }); %>
